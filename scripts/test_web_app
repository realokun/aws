#!/bin/bash
success=0
failure=1
result=$(curl -s http://localhost:80/)

if [[ "$result" =~ "Product Catalogs" ]]; then
    echo The web application has responded as expected!
    exit $succes
else
    echo The web application has not responded as expected!
    exit $failure
fi